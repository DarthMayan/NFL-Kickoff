apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: kickoff
  labels:
    app: user
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user
  template:
    metadata:
      labels:
        app: user
        tier: backend
    spec:
      containers:
      - name: user
        image: kickoff-user-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 9081
          name: grpc
          protocol: TCP
        env:
        - name: DB_NAME
          value: "user_db"
        envFrom:
        - configMapRef:
            name: postgres-config
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          grpc:
            port: 9081
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          grpc:
            port: 9081
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
